const superHeroes: string[] = [];

const heroPower: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

superHeroes.push("my wife");
heroPower.push(2);

allUsers.push({ name: "", isActive: true });

const MLModels: number[][] = [[255, 255, 255]];

export {};
